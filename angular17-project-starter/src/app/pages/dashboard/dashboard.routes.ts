import { Routes } from "@angular/router";

export const routes: Routes = [
    /** Example
    { path:'', component AdmindLayaoutComponent, children: [
        {path: '', component: DashboardComponent},
        {path: 'calendar', component: CalendarComponent},
    ]}
     */
]